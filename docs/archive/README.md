# Archive

Superseded design documents live here. Material is preserved rather than deleted — when a document
stops being current, it moves into this folder rather than disappearing, so the reasoning behind an
earlier decision remains readable.

Nothing here yet.
